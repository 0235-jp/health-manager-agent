{
  "name": "@health-manager/oura-ring",
  "version": "1.2.0",
  "description": "Oura Ring Data Source Plugin for Health Manager",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:zip": "npm run build && node scripts/create-zip.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "adm-zip": "^0.5.16"
  }
}
