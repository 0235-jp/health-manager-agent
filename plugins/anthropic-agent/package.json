{
  "name": "@health-manager/anthropic-agent",
  "version": "1.0.0",
  "description": "Anthropic Claude Agent Plugin for Health Manager",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:zip": "npm run build && node scripts/create-zip.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.3"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "adm-zip": "^0.5.16"
  }
}
