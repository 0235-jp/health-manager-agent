{
  "name": "@health-manager/line-chat-plugin",
  "version": "1.0.0",
  "description": "LINE Chat Plugin for Health Manager - Two-way communication via LINE Messaging API",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:zip": "npm run build && node scripts/create-zip.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^25.0.7",
    "adm-zip": "^0.5.16",
    "typescript": "^5.7.2"
  }
}
